/**
 * phi@smoch.ch
 * 2019-06-01
 */

.erfindung {
	color           : var(--default-textcolor );
	background-color: var(--default-background);
	margin-left     :  2px;
}

.erfindung h1 {
	font-style: italic;
	font-size : 112%  ;
	color     : var(--default-textcolor) ;
}

.erfindungHeader {
	display         : flex         ; /* establish flex container */
	flex-direction  : row          ; /* default value; can be omitted */
	flex-wrap       : nowrap       ; /* default value; can be omitted */
	justify-content : space-between; /* switched from default (flex-start, see below) */

	color           : var(--default-foreground)         ;
	background-color: var(--default-background)         ;
	text-align      : center       ;
	font-size       : 200%         ;
	font-weigth     : bold         ;
	font-variant    : small-caps   ;
	padding-top     : 5px          ;
}

.erfindungHeader > div {
  width  : 100px;
  height : auto ;
	padding: 3px  ;
}

.erfindungHeader > div:nth-child(2) {
	width: auto;
}

/*
.erfindungsBilder {
	color           : #08F;
	background-color: #fff;
}*/

.erfindungsText {
	clear: both;
}


/** TODO: Color als Konstante fixieren **/
h1.exponatListeTitel  {
	margin-top      : 28px     ;
	color           : var(--default-foreground)      ;
	width           : 100%     ;
	background-color: var(--exponat-background)    ;
	color           : var(--exponat-foreground-text)     ;
	font-variant    : normal   ;
	border-color    : var(--exponat-foreground-text)     ;
	border          : 2px solid;
	text-align      : center   ;
}


												
.erfindungsExponat {
	color           : var(--default-foreground);
	background-color: var(--default-background); 
}

tr.erfindungsExponat > td {
	background-color: var(--default-background);
	padding-left    : 15px;
	padding-right   : 15px;
	border          : 1px #f002 solid;
}

tr.exponatTabelle > td {
	background-color: var(--default-background);
	padding-left    : 15px ;
	padding-right   : 15px ;
	vertical-align  : top  ;
}


.small_image {
	width: 144px;
}

div.small_image_rahmen {
	padding: 2px;
	margin : 1px;
	border : solid 1px #f004;
}

.normal_image {
	margin :   1px;
	padding:   5px;
	width  : 500px;
}

div.erfindungsBilder{
/*	clear     :   both; */
	text-align: center;
}

div.erfindungsBild {
	margin-top      : 4px            ;
	padding         : 5px            ;
	margin          : 5px            ; 
	margin-bottom   : 2px            ;
	border          : solid #630  2px;
 	background-color: var(--exponat-background)           ;
	color           : var(--exponat-foreground-text)           ;
	font-weight     : bold           ;
	font-size       : 120%           ;
}
